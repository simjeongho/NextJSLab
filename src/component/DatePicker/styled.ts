import styled from '@emotion/styled';

export const DatePickerContainer = styled.div`
	background-color: ${({ theme: { colors } }) => colors.primary.sky};
	padding: 10px;
	display: inline-flex;
	border-radius: 10px;
`;

export const DatePickertitle = styled.div``;
